import React from 'react'
// import _Router from './Router/components/Router'
import Layout from './Layout/Layout'

const app = (
  <Layout />
);

class App extends React.Component {
  render() {
    return(
      app
    );
  }
}
export default App
